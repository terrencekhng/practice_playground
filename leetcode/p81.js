/**
 * @param {number[]} nums
 * @param {number} target
 * @return {boolean}
 */
var search = function(nums, target) {

};

let nums = [2,5,6,0,0,1,2];
let target = 0;
let result = search(nums);
console.log(result);
